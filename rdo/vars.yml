---
auth: {
    domain: .dlux.com,
    non_root_user: stack,
    password: secret9
}

openstack: {
    release: ocata
}

genv: {
    timeout: 1,
    proxy_env: {
        http_proxy: "http://poxy.example.com:8080",
        https_proxy: "http://proxy.example.com:8080"
    }
}

