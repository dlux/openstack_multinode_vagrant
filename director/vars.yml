---
auth: {
    domain: .dlux.com,
    non_root_user: stack,
    password: secret9,
}

openstack: {
    openstack_release: ocata
}

genv: {
    timeout: 1
    proxy_env:
        http_proxy: http://proxy.example.com:8080
}

